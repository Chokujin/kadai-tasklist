<% if users.any? %>
  <ul class="list-unstyled">
    <% users.each do |user| %>
      <div>
        <%= user.name %>
      </div>
      <div>
        <p><%= link_to 'View profile', user_path(user) %></p>
      </div>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>