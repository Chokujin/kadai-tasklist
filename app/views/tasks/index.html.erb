<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> &lt<%= task.status %>&gt : <%= task.content %> </li>
  <% end %>
  
  <%= paginate @tasks %>

  <%= link_to '新規タスク追加', new_task_path %>
  
</ul>